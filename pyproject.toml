[project]
name = "mcp-server-boilerplate"
version = "0.1.0"
description = "Minimal, reusable MCP server boilerplate with extensive documentation"
requires-python = ">=3.10"
dependencies = [
    "mcp>=0.9.0",
]
